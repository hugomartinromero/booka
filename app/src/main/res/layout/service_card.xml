<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <TextView
        android:id="@+id/lblServiceName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:layout_marginBottom="10dp"
        android:fontFamily="@font/montserrat_bold"
        android:text="@string/textview"
        android:textColor="@color/booka_text_primary"
        android:textSize="18sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/lblServiceDuration"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginEnd="30dp"
        android:fontFamily="@font/montserrat_bold"
        android:text="@string/textview"
        android:textColor="@color/booka_text_secondary"
        android:textSize="16sp"
        app:layout_constraintBottom_toBottomOf="@+id/lblServiceName"
        app:layout_constraintEnd_toStartOf="@+id/lblServicePrice"
        app:layout_constraintTop_toTopOf="@+id/lblServiceName" />

    <TextView
        android:id="@+id/lblServicePrice"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="@font/montserrat_bold"
        android:text="@string/textview"
        android:textColor="@color/booka_text_primary"
        android:textSize="18sp"
        app:layout_constraintBottom_toBottomOf="@+id/lblServiceName"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@+id/lblServiceName" />
</androidx.constraintlayout.widget.ConstraintLayout>